language: python

python:
- 2.7
- 3.4
- 3.5
- pypy
- pypy3

env:
- DJANGO_VERSION=1.8.4
- DJANGO_VERSION=1.9
- DJANGO_VERSION=1.10

matrix:
  allow_failures:
    - python: pypy
    - python: pypy3

install:
- pip install -q -r requirements.txt
- pip install -q coveralls codecov
- pip install -q Django==$DJANGO_VERSION

script: coverage run --source=quotes manage.py test

after_success:
- coveralls
- codecov
